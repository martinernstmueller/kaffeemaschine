namespace KaffeeMaschine
{
    public class Kaffeelager
    {
        public double lagerstand { get; set; }

        public Kaffeelager (double _lagerstand)
        {
            lagerstand = _lagerstand;
        }
    }
}
